{
  "name": "ste-madeleine",
  "private": true,
  "scripts": {
    "api": "docker-compose up backend --build",
    "api:reset": "docker-compose down --rmi all -v && docker-compose up backend --build",
    "dev": "concurrently \"npm run dev --prefix frontend/frontoffice\" \"npm run dev --prefix frontend/backoffice\"",
    "start": "concurrently \"npm run api\" \"npm run dev\""
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^8.2.2",
    "emoji-regex": "^10.3.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.12"
  }
}
